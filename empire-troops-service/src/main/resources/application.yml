server:
  port: 0

spring:
  application:
    name: troops

eureka:
  #  instance:
  #    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
#    serviceUrl:
#      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
